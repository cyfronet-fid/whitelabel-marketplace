- content_for :title, _("New Provider")
- breadcrumb :backoffice_provider_new, @provider

.container{ "data-controller": "form" }
  #form-container
    = turbo_frame_tag "provider-form" do
      = render_step(params[:id], @provider)
